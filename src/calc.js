let title = "CDAC";

export default function sum(a, b) {
  return a + b;
}

function diff(a, b) {
  return a - b;
}

function mul(a, b) {
  return a * b;
}

function div(a, b) {
  return a / b;
}

// export default sum;
export { diff, title };
